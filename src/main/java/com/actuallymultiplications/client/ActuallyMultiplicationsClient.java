package com.actuallymultiplications.client;

import com.actuallymultiplications.ActuallyMultiplications;

/**
 * Client-only hooks (renderers, keybinds, {@code @EventBusSubscriber(modid = …, value = Dist.CLIENT)}, etc.).
 */
public final class ActuallyMultiplicationsClient {

    private ActuallyMultiplicationsClient() {
    }
}
